
interface SidebarProps {
    isSidebarOpen: boolean;
    setIsSidebarOpen: React.Dispatch<React.SetStateAction<boolean>>;
    isMobile: boolean;
}

const UserDashboardSidebar = (_props: SidebarProps) => {
    return (
        <div>
            
        </div>
    );
}

export default UserDashboardSidebar;
